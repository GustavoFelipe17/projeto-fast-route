# Database - Local (para desenvolvimento)
DB_USER=postgres
DB_PASSWORD=10112005
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gestao_tarefas

# Database - Produção (Render fornece esta URL completa)
# Exemplo: postgres://user:password@host:5432/database
DATABASE_URL=

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (atualize com a URL real do seu frontend)
FRONTEND_URL=https://fastroute.netlify.app/

# Configurações adicionais para produção
# Render automaticamente define PORT, mas você pode sobrescrever
# RENDER_EXTERNAL_HOSTNAME será definido automaticamente pelo Render